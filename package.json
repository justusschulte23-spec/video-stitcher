{
  "name": "video-stitcher",
  "version": "1.0.0",
  "description": "Express + FFmpeg service to stitch multiple videos",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "postinstall": "apt-get update && apt-get install -y fonts-anton fontconfig && fc-cache -fv"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  }
}
